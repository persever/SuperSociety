SUNDAY:

- DATETIME PICKER
- format datetime on event index items and show
- preselect datetime
- pre-select group value in event edit form!!
- make errors on forms look a little nicer
- handle "enter" key from description textarea to submit form

- MAKE ALL LINKS WORK, BE ABLE TO WALK THROUGH EVERY ASPECT, IN AND OUT

- if there are no notes above this, update the readme time expectations and phase files, close the github issues, db reset both and push to both
- clean up your notes here and elsewhere (notebook, other file[s])
- MAKE GAMEPLAN THROUGH DEADLINE THURS!!! (Then time to do cracking the coding interview problems!!)

- BLOG POST

---

- basic clean coherent organization and design before details/reactivity/animation/etc
- guest login
- GROUP AND USER PAST EVENTS
- search should exclude past events
- 'no events' subview placeholder for groups with no events (like 'i have powers because science')

---

- make form error messages appear inside offending inputs? with red glow around offending box (that goes away on new input!)!
- group subscribe from index
- (ta help?) make logout button on nav work properly
- create members subview for group show
- CREATE USER MODEL???? (update phases) or just through the subscriptions, get the users' photos......
- make mouse note turn into text identifier on text hover... make it just be one of two things for clickable and not
- internal scroll: overflow y scroll to scroll
- make bootstrap grid for group index
- make bootstrap nav collapse at like 300px, no bigger

- in header bar on home page, put count of upcoming events (which switches in approp index subv)

- event show attenders

---

- get rid of currentUserManagedGroups assoc and any other related mess
- rename all "ssevent(s)" and all "event(s)" that are actually ssevents to "gatherings" or whateverfor comprehension
- why is server running/showing association things twice? low priority


- handle enter key in search
- handle enter ans esc keys on modal forms

- seed live action photos for users and groups

- root url background video behind semi-transparent black screen behind buttons if no current user

- guest signin autotype

- custom mouse image (white circle? comic book bang effect image?)

- make every route call redirect to signin page if no current user... currently tacks it on....

- make filepicker more site appropriate
- make appropriate rails route hits redirect to appropriate static page route
- make group genuine composite view, without that event show subview button navigating?

- allow user to change photo
- enable deletion of events and groups
> reset db every night at a weird hour! set enough seeds for a day of employers to delete!!!
- edit groups and events!!!
- ensure browser back button works :O

- add snippets to readme at very end
> filepicker redirect?
> frontend assocs? (toggle?)

- sign in and out and do things with multiple users, make sure that's not messing up

- clear out old unused files, unless useful to show process... but you have that in your readme so it probably just looks cluttered. it is just clutter.

- check out app in firefox

- search bar in nav if not on search page? (if on group page)

- clean up all comments, KEEP LINES WITHIN LENGTH LIMIT, refactor long and messy things, make sure naming is comprehensible

- super low priority: upcoming events count in nav

- note used bootstrap nav, search bar plugin, other big snipps
- REMOVE COMMENTS
- simplicity! tidiness!

Why did you call them events -_-
