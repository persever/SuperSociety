SUNDAY:

- buttons on home pages (showing counts!!) to users's upcoming events and subscribed groups
- init home page should show user's upcoming events

- basic organizational styling (mostly group index, group show, event show; then eii buttons)

- allow user to change photo from navbar (silhouette placeholder)
- now you can go back to a single signin page!!

- db reset both and push to both
- clean up your notes here and elsewhere (notebook, readme bonuses, other file[s])

- BLOG POST

- play around with Meetup more to feel out their mapping
- read that UI article

---

MONDAY:

- guest login
- search should exclude past events
- destroy groups and events
- user User model to get rid of currentUserManagedGroups assoc and any other related mess

- MAKE ALL LINKS WORK, BE ABLE TO WALK THROUGH EVERY SINGLE ASPECT, IN AND OUT, SMOOTHLY
- sign in and out and do things with multiple users, make sure that's not messing up
- check out app in firefox

TUESDAY:

- change all ssevent(s)/event(s) to their appropriate respective names so code is not confusing
- clean up all comments

- see "Patterns" and other performance optimization readings

- refactor long and messy things, and any logic that should be in a different file
- KEEP LINES WITHIN LENGTH LIMIT

- (ta help?) make logout button on nav work properly

- add snippets to readme

- auto reset heroku db every single night!
- a few more iron man seeds in case some employers go crazy!!

WEDNESDAY:

- internal scroll (overflow y scroll to scroll)
- slide in event index items (jQuery UI show effect)
- welcome screen video and epic music behind login/signup modal (heimdall + bridge video???)
- make errors on forms look a little nicer
- handle "enter" key from description textarea to submit forms
- deletion confirmation modal
- simple custom 404 pages
- 'no events' placeholder for groups with no events (like 'i have powers because science')
- make bootstrap nav collapse at like 300px, no bigger
- custom (simple circle) cursor for button/a/other clickable hover
- guest login autotype
- jQuery UI toggle class / color animation for smooth toggle button transitions
- FriendlyId
- dimensionality (diagonal slice on buttons, lighter above darker below)

- section off subviews like Meetup does, over background
- make event form modal not spazz its way on screen

THURSDAY:

- event comments

- homepage button for user's past events index
- tab on group page for past events index
- past event views should have a frozen attending button

- change signout button to power symbol
- make form error messages appear inside offending inputs? with red glow around offending box (that goes away on new input!)!
- logo
- reactive mouse
- make cloudinary more stylistically coherent
- catchy one-liner on entry page (def if you stuck with the split login/signup, maybe if just login/signup screen but don't clutter it)
- infinite scroll
- fade effect on every route change
- group subscribe from index
- remove button blue glow

- click on any piece of information to edit it

- group and event tags, searchable

---

FRIDAY:

Yay! Blog about it! And read the bonus material. Then do coding interview prep!
