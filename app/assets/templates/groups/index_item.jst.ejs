<div class="img-container">
  <img src="<%= group.escape("photo_url") %>" class="hoverable"></img>
  <div class="hide-on-hover">
    <img src="<%= group.escape("photo_url") %>" class="not-hoverable"></img>
    <div class="screen"></div>
    <div class="heading"><%= group.escape("name") %></div>
  </div>
</div>
